extends layout


block content
    h1 #{title}
    ul.list-group
        each book in books
                li.list-group-item
                    a(href= "/books/"+book._id)= book.title
                        